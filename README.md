# Tash: The easy, fast TensorFlow installation

Install/upgrade TensorFlow in one run for **Mac** and **Linux(_Ubuntu_)**

## Usage 

```cmd
git clone http://github.com/as1ndu/tash.git
cd tash

sudo bash tash
```

